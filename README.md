# demo-shop

Wegwerf-Shop als Bühne für die Rails-Screencast-Folgen (avp). Ein Produkt-Katalog,
sonst nichts — jede Folge fügt genau eine Technik hinzu.

Branches je Folge: `episode-NNN-before` / `episode-NNN-after`.

    bin/setup && bin/rails db:seed && bin/rails server
